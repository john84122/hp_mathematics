Notes on Files:

- Here are the breakdown of files I submitted for this assignment. All coding and csv files can be found on the github repo shown in the website.


CSV files for CPU timing runs for fractal generation:
    - cpu_fractal_runs2processes_timing.csv (Uses 2 proceses)
    - cpu_fractal_runs4processes_timing.csv (Uses 4 proceses)
    - cpu_fractal_runs8processes_timing.csv (Uses 8 proceses)
    - cpu_fractal_runs16processes_timing.csv (Uses 16 proceses)
    - cpu_fractal_runs32processes_timing.csv (Uses 32 proceses)

CSV Files for GPU timing runs for fractal generation:

    - gpu_global.csv

CSV Files for sequantial timing run for fractal generation:

    - sequential_run.csv

Files for generating fractals sequantiall, using CPUs, using GPU respectively:

    - fractal_sequential.py
    - fractal_parallel_cpu.py
    - fractal_single_gpu.py

Jupyter notebook for visualizing timings for runs to see how much of a speedup from code.

    - analysis_of_timing.ipynb

The writeup:

    - a_writeup_hpml_1.pdf