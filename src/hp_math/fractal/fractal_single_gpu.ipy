
import time
import numpy as np
from numba import njit, prange

@njit(parallel=True)
def paralell_fractal_generation(c):

    N = 100
    k = 100

    c_const = -2.1+ (-1.35j)

    iter = 0
    while (iter < N) and (np.abs(c) < k):
        c = np.sin(c) + c_const
        iter += 1

    return iter